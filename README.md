# Kombo — WordPress (`wp-content`)

Ovaj repozitorijum prati **samo** ono što tim razvija u okviru `wp-content/`:

| Putanja | Sadržaj |
|---------|---------|
| `themes/kombo/` | Parent tema (stanje sa lajva, dalji razvoj) |
| `themes/kombo-child/` | Child tema (`Template: kombo`) |
| `plugins/kombo-plugin/` | Custom plugin (ovde se dodaje logika) |

**Šta nije u Git-u:** ostali pluginovi (WooCommerce, Polylang, itd.), `uploads`, keš i ostalo — vidi `.gitignore`.

**WordPress:** aktivirati plugin *Kombo* u adminu kad bude spreman; teme kao i do sada (prvo parent, pa child).

---

*S7Codedesign — interni razvoj i održavanje.*
